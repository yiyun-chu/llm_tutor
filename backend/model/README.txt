Put your trained survival model here as survival_model.pkl
(or point SURVIVAL_MODEL_PATH at it). See ../feature_processing.py for how it's
loaded and called. If no file is present, the backend uses a transparent
heuristic so you can test the full pipeline before the model is ready.
